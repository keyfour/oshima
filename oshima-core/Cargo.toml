[package]
name = "oshima-core"
version = "0.1.0"
edition = "2021"
description = "Core traits for the Oshima actor framework (no_std compatible)"

[dependencies]
# No runtime deps — intentionally empty

[features]
default = ["std"]
std = []
